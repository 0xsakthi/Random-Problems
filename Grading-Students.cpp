#include <iostream>
using namespace std;

int main(){

	int n  = 4;
	arr[4] = {73,67,38,33};
	std::vector<int> v;
	for (int i = 0; i < n; ++i)
	{
		if (arr[i]>33)
		{
			continue;
		}
		int single = arr[i]%10;
		if (arr[i]==0 || arr[i]==5)
		{
			continue;
		}
		
	}

	return 0;
}